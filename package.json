{
  "devDependencies": {
    "turbo": "^1.10.6"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node apps/api/dist/main"
  },
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "@nestjs/config": "^3.0.0"
  }
}
